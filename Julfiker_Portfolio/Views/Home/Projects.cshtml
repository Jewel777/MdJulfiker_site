@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Projects";
}

<div class="container mt-5 mb-5">
  <h2 class="text-center fw-bold mb-4 text-warning">Projects</h2>

  <div class="project-stack">

    <!-- 1) 🌊 Scuba_Diving_Hawaii -->
    <div class="project-tilt" data-accent="#00bcd4" data-title="Scuba_Diving_Hawaii">
      <article class="project-card" tabindex="0" aria-label="Scuba_Diving_Hawaii project">
        <div class="project-head">
          <div class="project-logo emoji">🌊</div>
          <h5 class="project-title">Scuba_Diving_Hawaii</h5>
        </div>
        <p class="project-desc">
          Ruby on Rails 7 web app for scuba diving enthusiasts: location & tour listings, photo sharing,
          posts with comments/likes, and Devise-based authentication.
        </p>
        <div class="project-meta">
          <span class="badge-soft">Ruby on Rails 7</span>
          <span class="badge-soft">License: MIT</span>
        </div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 2) 🐢 turtle-mandala-art -->
    <div class="project-tilt" data-accent="#f37626" data-title="turtle-mandala-art">
      <article class="project-card" tabindex="0" aria-label="turtle-mandala-art project">
        <div class="project-head">
          <div class="project-logo emoji">🐢</div>
          <h5 class="project-title">turtle-mandala-art</h5>
        </div>
        <p class="project-desc">
          Python notebook that generates colorful turtle-inspired mandalas using overlapping circles and
          rotational symmetry.
        </p>
        <div class="project-meta">
          <span class="badge-soft">Python (Jupyter Notebook)</span>
        </div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 3) 🖥️ Python-based-Process-Scheduler-simulation -->
    <div class="project-tilt" data-accent="#00d4ff" data-title="Python-based-Process-Scheduler-simulation">
      <article class="project-card" tabindex="0" aria-label="Python-based-Process-Scheduler-simulation project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-cpu"></i></div>
          <h5 class="project-title">Python-based-Process-Scheduler-simulation</h5>
        </div>
        <p class="project-desc">
          Priority-based round-robin OS scheduler simulation: create, block, fork, terminate, and replace
          programs via interactive commands.
        </p>
        <div class="project-meta">
          <span class="badge-soft">Python</span>
          <span class="badge-soft">License: MIT</span>
        </div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 4) 🏗️ Tower-of-hanoi -->
    <div class="project-tilt" data-accent="#9b5cff" data-title="Tower-of-hanoi">
      <article class="project-card" tabindex="0" aria-label="Tower-of-hanoi project">
        <div class="project-head">
          <div class="project-logo emoji">🏗️</div>
          <h5 class="project-title">Tower-of-hanoi</h5>
        </div>
        <p class="project-desc">Classic recursive Tower of Hanoi implementation with step-by-step solution logic.</p>
        <div class="project-meta">
          <span class="badge-soft">Python</span>
          <span class="badge-soft">License: MIT</span>
        </div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 5) 🧮 MemoryAllocationSimulator -->
    <div class="project-tilt" data-accent="#00ffa6" data-title="MemoryAllocationSimulator">
      <article class="project-card" tabindex="0" aria-label="MemoryAllocationSimulator project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-hdd-network"></i></div>
          <h5 class="project-title">MemoryAllocationSimulator</h5>
        </div>
        <p class="project-desc">
          Simulates First Fit, Next Fit, Best Fit, and Worst Fit strategies; compares efficiency and fragmentation.
        </p>
        <div class="project-meta">
          <span class="badge-soft">Python</span>
          <span class="badge-soft">License: MIT</span>
        </div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 6) ⚡ QuickSort-Analysis -->
    <div class="project-tilt" data-accent="#ffd84d" data-title="QuickSort-Analysis">
      <article class="project-card" tabindex="0" aria-label="QuickSort-Analysis project">
        <div class="project-head">
          <div class="project-logo emoji">⚡</div>
          <h5 class="project-title">QuickSort-Analysis</h5>
        </div>
        <p class="project-desc">
          QuickSort implementation with benchmarking and performance visualization (Matplotlib);
          includes comparison against binary search.
        </p>
        <div class="project-meta">
          <span class="badge-soft">Python</span>
          <span class="badge-soft">License: MIT</span>
        </div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 7) 🍎 calorie-tracker -->
    <div class="project-tilt" data-accent="#57ff7b" data-title="calorie-tracker">
      <article class="project-card" tabindex="0" aria-label="calorie-tracker project">
        <div class="project-head">
          <div class="project-logo emoji">🍎</div>
          <h5 class="project-title">calorie-tracker</h5>
        </div>
        <p class="project-desc">Simple daily calorie tracker to log foods and compare intake against personal goals.</p>
        <div class="project-meta"><span class="badge-soft">Python</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 8) 🌊 Flood Prediction (Kanawha River) -->
    <div class="project-tilt" data-accent="#2fb1ff" data-title="Flood-Prediction-Intelligent-Flood-Forecasting-Model-Kanawha-River-WV-by-Machine-Learning">
      <article class="project-card" tabindex="0" aria-label="Flood-Prediction project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-droplet-half"></i></div>
          <h5 class="project-title">Flood-Prediction-Intelligent-Flood-Forecasting-Model-Kanawha-River-WV-by-Machine-Learning</h5>
        </div>
        <p class="project-desc">
          Long-term flood forecasting for the Kanawha River (WV) using LSTM, BiLSTM, GRU, and ARIMA for multi-level predictions.
        </p>
        <div class="project-meta"><span class="badge-soft">Python (Jupyter Notebook)</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 9) 🦷 Dental_Records -->
    <div class="project-tilt" data-accent="#a2ffe3" data-title="Dental_Records">
      <article class="project-card" tabindex="0" aria-label="Dental_Records project">
        <div class="project-head">
          <div class="project-logo emoji">🦷</div>
          <h5 class="project-title">Dental_Records</h5>
        </div>
        <p class="project-desc">Project related to dental record management or analysis. Details not publicly available.</p>
        <div class="project-meta"><span class="badge-soft">Private / Internal</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 10) 🏠 House Price Predictive Model -->
    <div class="project-tilt" data-accent="#ffb74d" data-title="House Price Predictive Model">
      <article class="project-card" tabindex="0" aria-label="House Price Predictive Model project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-house-door"></i></div>
          <h5 class="project-title">House Price Predictive Model</h5>
        </div>
        <p class="project-desc">Built linear &amp; logistic regression models to predict house prices from real-estate features.</p>
        <div class="project-meta"><span class="badge-soft">Python / Regression</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 11) 🧭 Expression Regression Model -->
    <div class="project-tilt" data-accent="#ff6ad5" data-title="Expression Regression Model">
      <article class="project-card" tabindex="0" aria-label="Expression Regression Model project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-diagram-3"></i></div>
          <h5 class="project-title">Expression Regression Model</h5>
        </div>
        <p class="project-desc">Used classification and dimensionality reduction to evaluate expression regression performance.</p>
        <div class="project-meta"><span class="badge-soft">ML / Classification &amp; DR</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 12) 📈 Google Stock Price Prediction — RNN -->
    <div class="project-tilt" data-accent="#4caf50" data-title="Google Stock Price Prediction — RNN">
      <article class="project-card" tabindex="0" aria-label="Google Stock Price Prediction — RNN project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-graph-up-arrow"></i></div>
          <h5 class="project-title">Google Stock Price Prediction — RNN</h5>
        </div>
        <p class="project-desc">Recurrent Neural Network trained on historical market data to forecast Google stock prices.</p>
        <div class="project-meta"><span class="badge-soft">Python / RNN</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 13) 🧩 Dimensionality Reduction & Unsupervised Methods -->
    <div class="project-tilt" data-accent="#9c27b0" data-title="Dimensionality Reduction & Unsupervised Methods">
      <article class="project-card" tabindex="0" aria-label="Dimensionality Reduction & Unsupervised Methods project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-layers"></i></div>
          <h5 class="project-title">Dimensionality Reduction &amp; Unsupervised Methods</h5>
        </div>
        <p class="project-desc">Clustering and feature embeddings to visualize and group unlabeled samples.</p>
        <div class="project-meta"><span class="badge-soft">ML / Clustering</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 14) 🧪 Prototypes & Click-through Demos — HCI -->
    <div class="project-tilt" data-accent="#7c9bff" data-title="Prototypes & Click-through Demos — HCI">
      <article class="project-card" tabindex="0" aria-label="Prototypes & Click-through Demos — HCI project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-box"></i></div>
          <h5 class="project-title">Prototypes &amp; Click-through Demos — HCI</h5>
        </div>
        <p class="project-desc">Interactive click-through demos for usability testing informed by HCI principles.</p>
        <div class="project-meta"><span class="badge-soft">UX / Prototyping</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

    <!-- 15) 🚢 Ship Booking Management Website (RESTORED) -->
    <div class="project-tilt" data-accent="#00e5ff" data-title="Ship Booking Management Website">
      <article class="project-card" tabindex="0" aria-label="Ship Booking Management Website project">
        <div class="project-head">
          <div class="project-logo icon"><i class="bi bi-ship"></i></div>
          <h5 class="project-title">Ship Booking Management Website</h5>
        </div>
        <p class="project-desc">Reservation site with integrated search and booking for ship trips.</p>
        <div class="project-meta"><span class="badge-soft">Web</span></div>
        <div class="project-glare"></div>
      </article>
      <div class="project-reflection"></div>
    </div>

  </div>
</div>

<style>
  .project-stack {
    display: grid;
    grid-template-columns: 1fr;
    gap: 18px;
  }

  .project-tilt {
    position: relative;
    --accent: #65d1ff; /* overridden per card */
    --glass: rgba(255,255,255,0.06);
    --glass-strong: rgba(255,255,255,0.075);
    --hairline: rgba(255,255,255,0.08);
    --hairline-strong: rgba(255,255,255,0.16);
  }

  .project-card {
    position: relative;
    background: var(--glass);
    backdrop-filter: saturate(140%) blur(14px);
    -webkit-backdrop-filter: saturate(140%) blur(14px);
    border: 1px solid var(--hairline);
    border-radius: 20px;
    padding: 20px 22px;
    box-shadow: 0 10px 28px rgba(0, 0, 0, 0.35);
    transform: perspective(900px) rotateX(0deg) rotateY(0deg) translateZ(0);
    transform-style: preserve-3d;
    transition: transform .2s ease, box-shadow .25s ease, border-color .25s ease, background .25s ease;
    will-change: transform;
    outline: none;
  }
  .project-card:hover,
  .project-card:focus-visible {
    background: var(--glass-strong);
    border-color: var(--hairline-strong);
    box-shadow: 0 18px 44px rgba(0, 0, 0, 0.45);
  }

  .project-head {
    display: flex; align-items: center; gap: 12px; margin-bottom: 6px;
  }
  .project-logo {
    width: 42px; height: 42px; display: grid; place-items: center;
    border-radius: 12px;
    background: radial-gradient(60% 60% at 30% 30%, rgba(255,255,255,.2), rgba(255,255,255,.05) 60%, transparent 100%),
                linear-gradient(135deg, rgba(255,255,255,.12), rgba(255,255,255,.02));
    border: 1px solid rgba(255,255,255,0.12);
    transform: translateZ(30px);
  }
  .project-logo.icon i { font-size: 1.25rem; color: #eaeaea; }
  .project-logo.emoji { font-size: 1.3rem; }

  .project-title { margin: 0; font-weight: 700; color: #eaeaea; letter-spacing: .2px; transform: translateZ(25px); }
  .project-desc  { margin: 8px 0 10px; color: rgba(255,255,255,0.78); transform: translateZ(15px); }
  .project-meta  { display: flex; flex-wrap: wrap; gap: 8px; transform: translateZ(10px); }

  .badge-soft {
    display: inline-block; padding: 6px 10px; border-radius: 999px;
    background: rgba(255,255,255,0.09);
    border: 1px solid rgba(255,255,255,0.14);
    color: rgba(255,255,255,0.92);
    font-size: .85rem; line-height: 1; user-select: none;
    backdrop-filter: blur(6px); -webkit-backdrop-filter: blur(6px);
  }

  /* Cursor-follow glare */
  .project-glare {
    pointer-events: none; position: absolute; inset: -1px; border-radius: inherit;
    background: radial-gradient(600px 200px at var(--mx,50%) var(--my,50%), rgba(255,255,255,.22), transparent 50%);
    opacity: 0; transition: opacity .2s ease; mix-blend-mode: screen; transform: translateZ(50px);
  }
  .project-card:hover .project-glare,
  .project-card:focus-visible .project-glare { opacity: 1; }

  /* Colored reflection (simple, Safari-friendly) */
  .project-reflection {
    pointer-events: none; position: relative; height: 16px; margin-top: 8px; filter: blur(8px);
    background: radial-gradient(50% 100% at 50% 0%, var(--accent), transparent 70%);
    opacity: .6;
  }

  /* Motion/Touch safety */
  @@media (prefers-reduced-motion: reduce) {
    .project-card { transition: none !important; }
    .project-glare { display: none !important; }
  }
  @@media (min-width: 768px) { .project-card { padding: 22px 24px; } }
</style>

<script>
  (function () {
    const isTouch = matchMedia('(pointer: coarse)').matches;
    const reduceMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;

    document.querySelectorAll('.project-tilt').forEach(wrapper => {
      const card  = wrapper.querySelector('.project-card');
      const glare = card.querySelector('.project-glare');
      const accent = wrapper.getAttribute('data-accent') || '#65d1ff';

      // feed accent into CSS var
      wrapper.style.setProperty('--accent', accent);

      // Skip 3D on touch devices / reduced motion
      if (isTouch || reduceMotion) return;

      const MAX_TILT = 10;  // degrees
      const ENTER_SCALE = 1.03;
      let raf = null;

      function update(e) {
        const r = card.getBoundingClientRect();
        const cx = r.left + r.width / 2;
        const cy = r.top  + r.height / 2;
        const x = (e.clientX - cx) / (r.width  / 2);
        const y = (e.clientY - cy) / (r.height / 2);
        const rotY = x * MAX_TILT;
        const rotX = -y * MAX_TILT;

        card.style.transform = `perspective(900px) rotateX(${rotX}deg) rotateY(${rotY}deg) scale(${ENTER_SCALE})`;

        if (glare) {
          const mx = ((e.clientX - r.left) / r.width)  * 100;
          const my = ((e.clientY - r.top)  / r.height) * 100;
          glare.style.setProperty('--mx', mx + '%');
          glare.style.setProperty('--my', my + '%');
        }
        raf = null;
      }

      function onMove(e){ if (!raf) raf = requestAnimationFrame(() => update(e)); }
      function onLeave(){ card.style.transform = 'perspective(900px) rotateX(0deg) rotateY(0deg) scale(1)'; }

      card.addEventListener('mousemove', onMove);
      card.addEventListener('mouseenter', () => {
        card.style.transition = 'transform .15s ease';
        setTimeout(() => (card.style.transition = ''), 160);
      });
      card.addEventListener('mouseleave', onLeave);
    });
  })();
</script>
